define([
  'backbone',
  'backbone.layoutmanager'
], function(Backbone) {

  Backbone.Layout.configure({
    manage: true

    //fetchTemplate: function(tmpl) {
      //return JST[tmpl];
    //}
  });

});
